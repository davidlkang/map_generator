[package]
name = "map_generator"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
noise = "0.7.0"  # Check for the latest version
image = "0.24.9"
rand = "0.8.5"
